test_env/

# Environment variables (contains secrets)
.env
.env.local
.env.*.local
frontend/coverage/

# Let's Encrypt certificates (generated on server)
docker/traefik/letsencrypt/acme.json

# Basic Auth credentials (sensitive)
docker/traefik/.htpasswd

# LEDES Converter data (SQLite runtime)
ferramentas/ledes-converter/data/*.db
