# Trello API Credentials
# Generate these at: https://trello.com/power-ups/admin
#
# CRITICAL: Never commit the actual .env file to version control!
# This is a template - copy to .env and fill in your actual credentials.

# Your Trello API Key (from Power-Up admin page)
TRELLO_API_KEY=your_api_key_here

# Your Trello API Token (generated via the authorization link on Power-Up page)
# Recommended: Set token to "never expire" to avoid authentication issues
TRELLO_API_TOKEN=your_token_here

# Optional: Set log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Rate limit configuration (requests per 10 seconds)
# Default: 90 (conservative, well below Trello's 100/10s per-token limit)
RATE_LIMIT_PER_10_SECONDS=90
